<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 主要是这个起作用,修改默认返回键的颜色-->
        <!--<item name="colorControlNormal">@android:color/white</item>-->
        <item name="android:textAllCaps">false</item><!--解决字母大写问题-->

    </style>
    <!--适配主界面-->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--使导航栏和状态栏可绘制（android5.0以上支持）,即状态栏是否覆盖在ContentView上，在styles.xml(v21)中设置-->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!--状态栏颜色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--使CoordinatorLayout、AppBarLayout及控件扩展到状态栏-->
    <style name="CommonNoActionBar_status_bar" parent="AppTheme.NoActionBar">

    </style>

</resources>
